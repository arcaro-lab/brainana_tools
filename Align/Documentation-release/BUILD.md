# Build

The app package is assembled from the recovered frontend distribution, readable local server and launcher, and official dual-architecture Node binaries. The readable v0.11.1 TypeScript project remains included as the reconstruction foundation.
